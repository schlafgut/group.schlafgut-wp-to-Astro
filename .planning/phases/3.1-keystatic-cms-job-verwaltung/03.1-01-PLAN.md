---
phase: 3.1-keystatic-cms-job-verwaltung
plan: 01
type: execute
---

<objective>
Keystatic CMS für Job-Verwaltung einrichten.

Purpose: Ermöglicht nicht-technischen Nutzern das Erstellen, Bearbeiten und Löschen von Stellenanzeigen über ein Admin-UI, ohne Code-Änderungen.
Output: Funktionierendes Keystatic Admin UI unter /keystatic mit Job Collection.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Keystatic Docs Summary:**
- Packages: `@keystatic/core @keystatic/astro @astrojs/react @astrojs/markdoc`
- Astro config: react(), markdoc(), keystatic() integrations
- Output mode: `hybrid` (für Admin UI Server-Rendering)
- Config: `keystatic.config.ts` im Root
- Admin UI: `/keystatic` Route

**Existierende Jobs (5 Stück):**
- ecommerce-manager
- erp-manager
- marktplatzmanager
- paid-social-marketer
- werkstudent-it

**Job-Datenstruktur (aus Original-HTML):**
- title: string (z.B. "E-Commerce Manager")
- gender: string (z.B. "m/w/d")
- employmentType: string (z.B. "Vollzeit")
- location: string (z.B. "München oder Eislingen")
- tasks: Markdoc (Aufgaben-Liste)
- requirements: Markdoc (Anforderungen-Liste)
- benefits: Markdoc (Benefits-Liste)
- contactEmail: string
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keystatic Dependencies und Astro Config</name>
  <files>src/package.json, src/astro.config.mjs</files>
  <action>
1. Installiere Dependencies:
   ```
   cd src && npm install @keystatic/core @keystatic/astro @astrojs/react @astrojs/markdoc
   ```

2. Update `astro.config.mjs`:
   - Importiere react, markdoc, keystatic
   - Füge integrations hinzu: react(), markdoc(), keystatic()
   - Setze `output: 'hybrid'` für Admin UI Server-Rendering
   - Behalte site und tailwind Config

WICHTIG: Die Reihenfolge der integrations ist: react() → markdoc() → tailwind() → keystatic()
  </action>
  <verify>npm run build sollte ohne Errors durchlaufen (Warnungen ok)</verify>
  <done>Dependencies installiert, astro.config.mjs mit hybrid output und allen integrations</done>
</task>

<task type="auto">
  <name>Task 2: Keystatic Config mit Jobs Collection</name>
  <files>src/keystatic.config.ts</files>
  <action>
Erstelle `src/keystatic.config.ts`:

```typescript
import { config, collection, fields } from '@keystatic/core';

export default config({
  storage: {
    kind: 'local',
  },
  collections: {
    jobs: collection({
      label: 'Stellenanzeigen',
      slugField: 'title',
      path: 'src/content/jobs/*',
      format: { contentField: 'tasks' },
      schema: {
        title: fields.slug({ name: { label: 'Jobtitel' } }),
        gender: fields.text({
          label: 'Geschlecht',
          defaultValue: 'm/w/d'
        }),
        employmentType: fields.select({
          label: 'Anstellungsart',
          options: [
            { label: 'Vollzeit', value: 'vollzeit' },
            { label: 'Teilzeit', value: 'teilzeit' },
            { label: 'Werkstudent', value: 'werkstudent' },
            { label: 'Praktikum', value: 'praktikum' },
          ],
          defaultValue: 'vollzeit'
        }),
        location: fields.text({
          label: 'Standort',
          defaultValue: 'Eislingen'
        }),
        tasks: fields.markdoc({
          label: 'Aufgaben',
          description: 'Deine Aufgaben in dieser Position'
        }),
        requirements: fields.markdoc({
          label: 'Anforderungen',
          description: 'Was bringst du mit?'
        }),
        benefits: fields.markdoc({
          label: 'Benefits',
          description: 'Warum wir?'
        }),
        contactEmail: fields.text({
          label: 'Bewerbungs-Email',
          defaultValue: 'bewerbung@schlafgut.com'
        }),
        published: fields.checkbox({
          label: 'Veröffentlicht',
          defaultValue: true
        }),
      },
    }),
  },
});
```

WICHTIG:
- `path` zeigt auf `src/content/jobs/*` wo Astro Content Collections die Daten erwartet
- `format: { contentField: 'tasks' }` macht das Hauptfeld zum Markdoc-Content
  </action>
  <verify>TypeScript sollte keine Errors zeigen, File existiert</verify>
  <done>keystatic.config.ts mit vollständiger Jobs Collection Schema</done>
</task>

<task type="auto">
  <name>Task 3: Content Collection Config für Astro</name>
  <files>src/src/content/config.ts, src/src/content/jobs/.gitkeep</files>
  <action>
1. Erstelle Verzeichnis `src/src/content/jobs/`

2. Erstelle `src/src/content/config.ts`:

```typescript
import { defineCollection, z } from 'astro:content';

const jobs = defineCollection({
  type: 'content',
  schema: z.object({
    title: z.string(),
    gender: z.string().default('m/w/d'),
    employmentType: z.enum(['vollzeit', 'teilzeit', 'werkstudent', 'praktikum']).default('vollzeit'),
    location: z.string(),
    requirements: z.string().optional(),
    benefits: z.string().optional(),
    contactEmail: z.string().email().default('bewerbung@schlafgut.com'),
    published: z.boolean().default(true),
  }),
});

export const collections = { jobs };
```

3. Erstelle `.gitkeep` in `src/src/content/jobs/` damit das Verzeichnis im Git bleibt

WICHTIG: Das Schema hier muss mit keystatic.config.ts übereinstimmen (ohne das tasks-Feld, das ist der Content)
  </action>
  <verify>npm run build ohne Content Collection Errors</verify>
  <done>Astro Content Collection für Jobs konfiguriert, Verzeichnis existiert</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd src && npm run build` erfolgreich
- [ ] `cd src && npm run dev` startet ohne Errors
- [ ] `/keystatic` Route zeigt Admin UI
- [ ] Jobs Collection ist im Admin UI sichtbar
- [ ] Neuer Job kann erstellt werden (Test-Eintrag)
</verification>

<success_criteria>
- Keystatic Admin UI unter /keystatic erreichbar
- Jobs Collection mit allen Feldern konfiguriert
- Astro Content Collection für Jobs eingerichtet
- Build erfolgreich
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/3.1-keystatic-cms-job-verwaltung/03.1-01-SUMMARY.md` erstellen

Nächster Plan: 03.1-02-PLAN.md (Job-Seiten mit Content Collection rendern)
</output>
