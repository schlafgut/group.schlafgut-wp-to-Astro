---
interface Props {
  class?: string;
  animate?: boolean;
}

const { class: className = "", animate = true } = Astro.props;
---

<section class:list={["section__teaser-blocks", "default", className, { "stagger-container": animate }]}>
  <div class="innerwrapper">
    <slot />
  </div>
</section>
